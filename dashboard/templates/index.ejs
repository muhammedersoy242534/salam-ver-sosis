<%- include('blocks/header', {bot, user, path}) %>
  <div class="jumbotron">
    <div>
      <h1><%= bot.user.username %> Kontrol Paneli</h1>
      <h4><% if(bot.appInfo.description.length > 3) { %> <%= bot.appInfo.description %> <% } else { %>ADL BOT Kontrol Paneline Hoş Geldiniz Bot İle Alakalı çoğu şeyi buradan kolayca yönetebilirsiniz.Size sunacağımız hizmetin elimizden geldikçe en iyisi olmasını istiyoruz.<% }%></h4>
      <p><a class="btn btn-primary" href="/commands" role="button">Komutlar »</a></p>
    </div>
  </div>
<% include ./blocks/footer %>